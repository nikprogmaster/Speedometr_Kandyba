<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SpeedometrViewDefault">
        <item name="speed">0</item>
        <item name="max_speed">200</item>
        <item name="low_speed_color">@color/green</item>
        <item name="medium_speed_color">@color/yellow</item>
        <item name="high_speed_color">@color/red</item>
        <item name="arrow_color">@color/black</item>
    </style>
</resources>
